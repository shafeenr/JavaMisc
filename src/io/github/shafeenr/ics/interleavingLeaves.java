package io.github.shafeenr.ics;

import java.util.Scanner;

public class interleavingLeaves {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int scenarios = in.nextInt();

        for (int i = 0; i < scenarios; i++) {
            String[] leaves = new String[in.nextInt()];
            for (String s : leaves) {
                s = in.nextLine();
            }
            String output = "";
            int position = 1;
            int x;
            for (x = 0; x < leaves.length; x++) {

            }
        }
    }
}
